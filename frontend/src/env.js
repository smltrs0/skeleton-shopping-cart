const API_URL = "http://localhost/api/";

const IVA = 1.16;

const weightPerItem = 0.2; // Peso por camiseta en kg (200 gramos)

const shippingRatePerKg = 5; // Tarifa de envío por kg en USD


export {
      API_URL, IVA,
      weightPerItem,
      shippingRatePerKg,
};
